apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
configMapGenerator:
- literals:
  - DB_USER=di
  - DB_PASSWORD=vault:kv/secret/data/dev/di-entry/app#DB_PASSWORD
  - DB_HOST=di-entry-psql
  - DB_PORT=5432
  - DB_DATABASE=di_entry
  name: di-entry-config
  behavior: merge
